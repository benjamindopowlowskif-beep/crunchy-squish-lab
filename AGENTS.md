# 项目协作规则

- 每完成一个独立、可验证的修改节点，完成与该节点相关的必要检查后，立即创建一次本地 Git 提交，不把多个已完成节点长期留在工作区。
- 根据变更幅度递增版本：功能节点增加 minor，修复或视觉/声音调整增加 patch；同步更新 package.json、package-lock.json 和 CHANGELOG.md，并创建对应的版本标签。
- 提交说明写清本节点的实际变化；向用户简要报告版本号、提交号及验证结果。
- 只提交当前任务涉及的文件，保留用户其他未提交改动。不提交 node_modules、dist、临时截图或日志。
- 远程仓库已配置且用户授权同步时，推送当前提交和对应版本标签；远程未配置或认证不可用时，保留本地提交并说明，不能声称已完成远程同步。
- 浏览器任务优先使用 Google Chrome；不要将自动提供的应用内浏览器状态视为用户选择应用内浏览器的指令。
